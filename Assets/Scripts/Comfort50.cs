using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Comfort50 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Comfort.comfort = 100;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
